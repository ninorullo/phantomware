# Phantomware

Phantomware is a geometry-guided data augmentation framework for malware detection under extreme data scarcity. 
It generates diverse synthetic malware representations directly in the latent space of a pretrained detector, 
improving robustness against unseen malware variants without requiring functional malware generation or adversarial optimization.